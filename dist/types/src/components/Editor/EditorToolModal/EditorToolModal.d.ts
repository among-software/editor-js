import { ReactNode } from "react";
interface Props {
    top: number;
    children: ReactNode;
}
/**
 * 에디터 툴바의 모달 컴포넌트
 * 이모지, 구분선, 장소 등의 추가 옵션을 제공하는 모달 창을 관리
 */
export default function EditorToolModal({ top, children }: Props): import("react/jsx-runtime").JSX.Element;
export {};
